<style is:global lang="scss">
  @tailwind base;
  @tailwind components;
  @tailwind utilities;

  html {
    scroll-behavior: smooth;
  }

  * {
    font-family: "Inter";
  }

  .border-bottom {
    border-bottom: 1px solid rgb(255, 255, 255, 0.1);
  }

  .link {
    @apply no-underline hover:decoration-primary hover:decoration-solid hover:underline hover:underline-offset-2;
  }

  .cd-font-color {
    @apply font-bold text-primary;
  }

  .angular-font-color {
    background: #e30a7b;
    background: linear-gradient(to right, #e30a7b 0%, #584bcd 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    @apply font-bold;
  }

  .page-section {
    @apply py-24;

    &:nth-child(2n) {
      @apply bg-[#f3f3f3];
    }
  }

  .section-title {
    @apply font-bold text-3xl;
  }

  .section-description {
    @apply text-lg leading-relaxed;
  }

  .hero-title {
    @apply text-4xl sm:text-4xl 3xl:text-5xl font-bold leading-tight;
  }

  .hero-description {
    @apply text-base sm:text-lg 3xl:text-xl;
  }

  .hint-text {
    @apply text-xs text-gray-500;
  }

  .custom-card {
    @apply border shadow-xl bg-white rounded-lg p-8;
  }
</style>
