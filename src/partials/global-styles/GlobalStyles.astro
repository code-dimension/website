<style is:global lang="scss">
  @tailwind base;
  @tailwind components;
  @tailwind utilities;

  html {
    scroll-behavior: smooth;
  }

  * {
    font-family: "Inter";
  }

  .border-bottom {
    border-bottom: 1px solid rgb(255, 255, 255, 0.1);
  }

  .link {
    @apply no-underline hover:decoration-primary hover:decoration-solid hover:underline hover:underline-offset-2;
  }

  .bg-gray {
    @apply bg-[#f3f3f3];
  }

  .underlined {
    /*   background: red; */
    position: relative;
    &:after {
      content: "";
      position: absolute;
      top: 105%;
      width: 100%;
      aspect-ratio: 3 / 1;
      left: 50%;
      transform: translate(-50%, 0);
      border-width: 2px;
      border-style: solid;
      --spread: 140deg;
      --start: 290deg;
      mask: conic-gradient(
        from var(--start),
        white 0 var(--spread),
        transparent var(--spread)
      );
    }

    &.underlined-red::after {
      border-color: rgba(178, 0, 18, 0.8);
    }

    &.underlined-code::after {
      border-color: #6657ec;
    }

    &.underlined-green::after {
      border-color: #1a5d1a;
    }
  }
</style>
